# ignore everything by default
/*

# keep these two folders tracked
!/mods/
!/config/

# within tracked folders, ignore common junk
mods/*.old
mods/*.disabled
mods/~$*

# launcher/download caches sometimes placed inside config
config/**/cache/
config/**/.cache/

# OS junk
.DS_Store
Thumbs.db

# logs and crash reports (if they end up here)
logs/
crash-reports/
